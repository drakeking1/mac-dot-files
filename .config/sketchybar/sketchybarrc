#!/bin/bash

source "$CONFIG_DIR/colors.sh" # Loads all defined colors

ITEM_DIR="$CONFIG_DIR/items" # Directory where the items are configured
PLUGIN_DIR="$CONFIG_DIR/plugins" # Directory where all the plugin scripts are stored

FONT="JetBrainsMono Nerd Font" # Text font (all labels)
ICON_FONT="SF Pro" # Icon font — SF Symbols only render in SF Pro
PADDINGS=1 # All paddings use this value (icon, label, background)

# Setting up and starting the helper process for the CPU item
HELPER=sketchybar.cup.helper
killall helper
(cd "$CONFIG_DIR"/helper && make)
"$CONFIG_DIR"/helper/helper $HELPER > /dev/null 2>&1 &

# Setting up the general bar appearance of the bar
bar=(
    height=32
    color="$BAR_COLOR"
    border_width=0
    border_color="$BAR_BORDER_COLOR"
    shadow=off
    position=top
    padding_right=4
    padding_left=4
    sticky=on
    topmost=window
    show_in_fullscreen=on
    margin=6
)

sketchybar --bar "${bar[@]}"

# Setting up default values
defaults=(
    updates=when_shown
    icon.font="$ICON_FONT:Bold:14.0"
    icon.color="$ICON_COLOR"
    icon.padding_left="$PADDINGS"
    icon.padding_right="$PADDINGS"
    label.font="$FONT:Semibold:13.0"
    label.color="$LABEL_COLOR"
    label.padding_left="$PADDINGS"
    label.padding_right="$PADDINGS"
    padding_right="$PADDINGS"
    padding_left="$PADDINGS"
    background.height=26
    background.corner_radius=0
    background.border_width=0
    popup.background.border_width=0
    popup.background.corner_radius=0
    popup.background.border_color="$POPUP_BORDER_COLOR"
    popup.background.color="$POPUP_BACKGROUND_COLOR"
    popup.blur_radius=20
    popup.background.shadow.drawing=on
    scroll_texts=on
)

sketchybar --default "${defaults[@]}"

# Left
# source "$ITEM_DIR/apple.sh"
source "$ITEM_DIR/spaces.sh"
source "$ITEM_DIR/front_app.sh"

# Center
source "$ITEM_DIR/clock.sh"


# Center

# Right
# source "$ITEM_DIR/calendar.sh"   # file removed
source "$ITEM_DIR/wifi.sh"
source "$ITEM_DIR/battery.sh"
source "$ITEM_DIR/volume.sh"
source "$ITEM_DIR/media.sh"
# source "$ITEM_DIR/events.sh"   # calendar icon — disabled
# source "$ITEM_DIR/ram.sh"   # file removed
# source "$ITEM_DIR/cpu.sh"   # file removed

sketchybar --hotload off

# Forcing all item scripts to run (never do this outside of sketchybarrc)
sketchybar --update

echo "sketchybar configuation loaded.."
